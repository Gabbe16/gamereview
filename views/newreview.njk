{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
  <h1>Som kattägare vill jag att min katt också ska få vara med</h1>
  <p>Formuläret visas på sidan /newcat, men när formuläret skickas så används metoden POST för att skicka det till /cats</p>
  <form action="/reviews" method="post">
    <div class="form-element">
      <label for="review">Recension:</label>
      <input type="text" name="review" id="review">
    </div>
    <div class="form-element">
      <label for="score">Score (Från 1-10):</label>
      <input type="number" name="score" id="score">
    </div>
    <div class="form-element">
      <label for="game">Spel:</label>
      <select name="game" id="game">
        {% for game in games %}
          <option value="{{ game.id }}">{{ game.name }}</option>
        {% endfor %}
      </select>
    </div>
    <div class="form-element">
      <input type="submit" value="Lägg till din recension">
    </div>
  </form>
</main>
{% endblock %}